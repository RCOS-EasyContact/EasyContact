# Log level
logging:
  level:
    root: INFO
    com.huytmb: DEBUG
    org.springframework.integration: DEBUG

# Mail
mail:
  imap:
    ssl: true
    host: mail.rpi.edu
    port: 993
    username: gongz3
    password: BoFYmBTNcTN1R6mM

# Task execution
spring:
  task:
    execution:
      thread-name-prefix: autohost-task-
      pool:
        core-size: 2
        max-size: 200
        queue-capacity: 10000
    scheduling:
      thread-name-prefix: mail-receiver-
      pool:
        size: 2
